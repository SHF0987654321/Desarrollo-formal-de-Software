spring:
  application:
    name: gateway-server # Nombre con el que el Config Server buscará la configuración (gateway-server.yml)
  config:
    import: configserver:config-server # El Gateway se conecta al Config Server via Eureka